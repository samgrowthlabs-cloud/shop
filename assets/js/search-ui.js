import{SHOPLAB_CONFIG as C}from'./config.js';
const RECENT_KEY='shoplab-recent-searches';
let controller=null,activeIndex=-1;
const escapeHtml=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
const recent=()=>{try{return JSON.parse(localStorage.getItem(RECENT_KEY)||'[]').slice(0,6)}catch{return[]}};
function saveRecent(query){const clean=query.trim().slice(0,100);if(clean.length<2)return;localStorage.setItem(RECENT_KEY,JSON.stringify([clean,...recent().filter(item=>item.toLowerCase()!==clean.toLowerCase())].slice(0,6)))}
async function api(path,signal){const response=await fetch(`${C.API_BASE_URL}${path}`,{signal});if(!response.ok)throw new Error('Busca indisponível');const json=await response.json();return json.data||[]}
function highlight(text,query){const safe=escapeHtml(text),terms=query.trim().split(/\s+/).filter(Boolean).map(term=>term.replace(/[.*+?^${}()|[\]\\]/g,'\\$&'));if(!terms.length)return safe;return safe.replace(new RegExp(`(${terms.join('|')})`,'gi'),'<mark>$1</mark>')}
function attach(form){if(form.dataset.searchReady)return;form.dataset.searchReady='true';form.classList.add('search-enhanced');const input=form.querySelector('input[name=q]');if(!input)return;const panel=document.createElement('div');panel.className='search-panel';panel.setAttribute('role','listbox');form.append(panel);let timer;
  const close=()=>{panel.classList.remove('open');activeIndex=-1};
  const renderRecent=()=>{const items=recent();if(!items.length)return close();panel.innerHTML=`<div class="search-panel-title">Pesquisas recentes <button type="button" data-clear>Limpar</button></div>${items.map(item=>`<a role="option" href="busca.html?q=${encodeURIComponent(item)}"><span class="history-icon">↺</span>${escapeHtml(item)}</a>`).join('')}`;panel.classList.add('open')};
  const load=async()=>{const query=input.value.trim();if(query.length<2)return renderRecent();controller?.abort();controller=new AbortController();panel.innerHTML='<div class="search-panel-loading">Buscando…</div>';panel.classList.add('open');try{const items=await api(`/api/v1/search/suggestions?q=${encodeURIComponent(query)}`,controller.signal);panel.innerHTML=items.length?items.map(item=>{const image=item.storageKey?`${C.API_BASE_URL}/media/${encodeURIComponent(item.storageKey)}`:item.externalUrl||'';return `<a class="suggestion-product" role="option" href="produto.html?slug=${encodeURIComponent(item.slug)}">${image?`<img src="${escapeHtml(image)}" alt="${escapeHtml(item.altText||item.name)}" loading="lazy">`:'<span class="suggestion-placeholder">⌬</span>'}<span class="suggestion-copy"><span class="suggestion-main">${highlight(item.name,query)}</span><small>${escapeHtml([item.category,item.brand].filter(Boolean).join(' · '))}</small></span></a>`}).join(''):`<a role="option" href="busca.html?q=${encodeURIComponent(query)}"><span class="suggestion-main">Pesquisar por “${escapeHtml(query)}”</span><small>Ver resultados e alternativas</small></a>`}catch(error){if(error.name!=='AbortError')panel.innerHTML='<div class="search-panel-loading">Não foi possível carregar sugestões.</div>'}};
  input.addEventListener('focus',()=>input.value.trim().length>=2?load():renderRecent());
  input.addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(load,220)});
  input.addEventListener('keydown',event=>{const options=[...panel.querySelectorAll('[role=option]')];if(event.key==='Escape')return close();if(!['ArrowDown','ArrowUp','Enter'].includes(event.key)||!options.length)return;if(event.key==='Enter'&&activeIndex<0)return;event.preventDefault();activeIndex=event.key==='ArrowDown'?Math.min(activeIndex+1,options.length-1):event.key==='ArrowUp'?Math.max(activeIndex-1,0):activeIndex;options.forEach((option,index)=>option.classList.toggle('active',index===activeIndex));if(event.key==='Enter')options[activeIndex].click()});
  form.addEventListener('submit',()=>saveRecent(input.value));
  panel.addEventListener('click',event=>{if(event.target.closest('[data-clear]')){localStorage.removeItem(RECENT_KEY);panel.innerHTML='';close()}});
  document.addEventListener('click',event=>{if(!form.contains(event.target))close()});
}
async function loadTrending(){const container=document.querySelector('.trending');if(!container||container.dataset.real)return;container.dataset.real='true';try{const items=await api('/api/v1/search/trending');if(items.length)container.innerHTML=`<strong>Em alta:</strong>${items.slice(0,6).map(item=>`<a href="busca.html?q=${encodeURIComponent(item.query)}">${escapeHtml(item.query)}</a>`).join('')}`}catch{}}
function scan(){document.querySelectorAll('form.search').forEach(attach);loadTrending()}
new MutationObserver(scan).observe(document.documentElement,{childList:true,subtree:true});
document.readyState==='loading'?document.addEventListener('DOMContentLoaded',scan):scan();

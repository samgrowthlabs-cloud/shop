import {SHOPLAB_CONFIG as C} from './config.js';
const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function api(path,opt={}){const form=opt.body instanceof FormData,res=await fetch(C.API_BASE_URL+path,{...opt,credentials:'include',headers:{...opt.headers,...(!form&&opt.body?{'Content-Type':'application/json'}:{})}}),json=await res.json();if(res.status===401){location.href='login.html';throw Error('Sessao expirada')}if(!res.ok||!json.success)throw Error(json.error?.message||'Erro ao carregar');return json.data}
const inventory={desktop:[['top','Top banner (global)','728x90',['all']],['home_categories','Entre categorias (home)','728x90',['home']],['home_sections','Entre secoes (home)','728x90',['home']],['grid_sidebar','Acima dos produtos (catalogo)','728x90',['products','category']],['footer','Antes do rodape','728x90',['all']],['product_top','Pagina de produto (topo)','728x90',['product']],['product_middle','Pagina de produto (meio)','728x90',['product']]],mobile:[['menu','Abaixo do menu','320x50',['all']],['products','Entre produtos','320x50',['home','products','category']],['sections','Entre secoes','320x50',['home']],['footer','Antes do rodape','320x50',['all']],['product_top','Topo da pagina (produto)','320x50',['product']],['product_middle','Meio da pagina (produto)','320x100',['product']],['product_footer','Antes do rodape (produto)','320x50',['product']]]};
let device='desktop',pageKind='home',ads=[],selectedAd='',draggingAd='',placements={desktop:{},mobile:{}};
const placementMap=(targetDevice=device,targetPage=pageKind)=>((placements[targetDevice][targetPage]??={}),placements[targetDevice][targetPage]);
const currentSlots=()=>inventory[device].filter(([, , ,pages])=>pages.includes('all')||pages.includes(pageKind));
function sidebar(){return `<aside class="sidebar"><a class="logo" href="../index.html">SHOP<b>LAB</b></a><nav><div class="nav-group"><span>Vis&atilde;o geral</span><a href="index.html">Painel</a><a href="usuarios.html">Usu&aacute;rios</a></div><div class="nav-group"><span>Cat&aacute;logo</span><a href="produtos.html">Produtos</a><a href="categorias.html">Categorias</a><a href="colecoes.html">Cole&ccedil;&otilde;es</a><a href="marcas.html">Marcas</a><a href="parceiros.html">Lojas parceiras</a></div><div class="nav-group"><span>Marketing</span><a href="promocoes.html">Promo&ccedil;&otilde;es</a><a href="banners.html">Banners</a><a href="destaque-cabecalho.html">Destaques do cabe&ccedil;alho</a><a class="active" href="anuncios-cabecalho.html">An&uacute;ncios / Propagandas</a></div><div class="nav-group"><span>Intelig&ecirc;ncia artificial</span><a href="ia.html">Modelos por fun&ccedil;&atilde;o</a></div><div class="nav-group"><span>Premium</span><a href="premium.html">Plano, pre&ccedil;o e promo&ccedil;&atilde;o</a></div><div class="nav-group"><span>Equipe</span><a href="colaboradores.html">Colaboradores</a></div><div class="nav-group"><span>Apar&ecirc;ncia</span><a href="temas.html">Temas sazonais</a></div></nav><a class="admin-public-link" href="../index.html" target="_blank">Ver site p&uacute;blico &nearr;</a></aside>`}
function shell(){document.body.innerHTML=`<div class="admin-shell">${sidebar()}<main class="admin-main ads-editor"><header class="admin-top"><div><span class="eyebrow">ADMINISTRA&Ccedil;&Atilde;O &nbsp;&rsaquo;&nbsp; AN&Uacute;NCIOS &nbsp;&rsaquo;&nbsp; POSI&Ccedil;&Otilde;ES</span><h1>Shoplab Ads</h1><p class="muted">Arraste os an&uacute;ncios para os espa&ccedil;os reais do site.</p></div><div class="admin-actions"><button class="btn ghost" id="refresh">Atualizar preview</button><a class="btn ghost" href="../index.html" target="_blank">Ver site p&uacute;blico</a><button class="btn primary" id="save">Salvar altera&ccedil;&otilde;es</button></div></header><div id="message" class="admin-message"></div><div class="ads-device-toggle"><button type="button" data-device="desktop" class="active">Desktop</button><button type="button" data-device="mobile">Mobile</button></div><section class="ads-layout"><aside class="ads-library"><div class="section-head"><div><span class="eyebrow">SEUS AN&Uacute;NCIOS</span><h2>Arraste para o site</h2></div></div><div class="ads-filter"><button class="active">Todos</button><button>Ativos</button><button>Pausados</button></div><div id="ads"></div><button id="new" class="btn ghost ads-new">+ Criar novo an&uacute;ncio</button></aside><section class="ads-stage"><div class="ads-live"><span>● Preview em tempo real</span><select id="page"><option value="index.html" data-kind="home">In&iacute;cio</option><option value="produtos.html" data-kind="products">Produtos</option><option value="categoria.html?slug=tecnologia" data-kind="category">Categoria</option><option value="produto.html?slug=habitos-atomicos" data-kind="product">Produto</option></select></div><div class="ads-frame is-desktop" id="frame"><iframe id="site" src="../index.html" title="Preview real do site"></iframe></div></section><aside class="ads-positions"><div class="section-head"><div><span class="eyebrow">INVENT&Aacute;RIO</span><h2>Posi&ccedil;&otilde;es dispon&iacute;veis</h2><p class="muted">Somente os espa&ccedil;os desta p&aacute;gina.</p></div></div><div id="position-list"></div></aside></section></main></div><div class="modal" id="modal"><div class="modal-card"><div class="modal-head"><h2 id="modal-title">Novo an&uacute;ncio</h2><button class="icon-btn" id="close" type="button">&times;</button></div><form id="form" class="form-grid"><input id="editId" type="hidden"><div class="form-field"><label>Nome do an&uacute;ncio</label><input id="name" required placeholder="Promo&ccedil;&atilde;o de notebooks"></div><div class="form-field"><label>Tipo de m&iacute;dia</label><select id="mediaType"><option>Imagem</option><option>V&iacute;deo</option></select></div><div class="form-field full"><label>Palavras-chave de interesse</label><input id="targetKeywords" maxlength="300" placeholder="Ex.: notebook gamer, placa de v�deo, RTX"><small>Separe por v�rgulas. Para usu�rios logados, pesquisas recentes relacionadas priorizam este an�ncio.</small></div><div class="form-field full"><label>Título público</label><input id="publicTitle" maxlength="140" placeholder="Ex.: Galaxy A17 em oferta"><small>Este título aparece acima da mídia.</small></div><div class="form-field full"><label>Identificação da publicidade</label><input id="adLabel" maxlength="80" value="PUBLICIDADE · SHOPLAB ADS"></div><div class="form-field"><label>Texto do botão</label><input id="ctaText" maxlength="40" value="Saiba mais" placeholder="Ex.: Comprar agora"></div><div class="form-field"><label>Cor do botão</label><input id="ctaColor" type="color" value="#075fce"></div><div class="form-field"><label>Valor antigo</label><input id="oldPriceText" maxlength="32" placeholder="Ex.: R$ 1.999,00"></div><div class="form-field"><label>Cor do valor antigo</label><input id="oldPriceColor" type="color" value="#71807c"></div><div class="form-field"><label>Valor atual</label><input id="currentPriceText" maxlength="32" placeholder="Ex.: R$ 1.499,00"></div><div class="form-field"><label>Cor do valor atual</label><input id="currentPriceColor" type="color" value="#087c70"></div><div class="form-field switch-field"><label><input id="showHeader" type="checkbox" checked> Mostrar título e identificação</label></div><div class="form-field switch-field"><label><input id="dismissible" type="checkbox" checked> Permitir que o usuário feche</label></div><div class="form-field"><label>Voltar depois de quantos minutos?</label><input id="dismissMinutes" type="number" min="1" max="10080" value="30"><small>Máximo: 7 dias (10.080 minutos).</small></div><div class="form-field full"><label>Arquivo</label><input id="media" type="file" accept="image/*,video/mp4,video/webm"></div><div class="form-field full"><label>Produto da SHOPLAB (opcional)</label><input id="productSlug" list="shoplab-product-options" placeholder="Digite o nome ou slug do produto"><datalist id="shoplab-product-options"></datalist><small>Digite para pesquisar. Produto vinculado atualiza pre�o e link automaticamente.</small></div><div class="form-field full"><label>URL externa de destino</label><input id="link" required value="promocoes.html"></div><div class="form-field full ads-distribution"><label>Distribui&ccedil;&atilde;o do an&uacute;ncio</label><select id="distributionMode"><option value="manual">Manual &mdash; eu escolho e removo as p&aacute;ginas</option><option value="weighted">Autom&aacute;tica por peso</option></select><small>No modo manual, use o preview para colocar ou tirar o an&uacute;ncio de cada posi&ccedil;&atilde;o.</small></div><div class="form-field full" id="weightField" hidden><label>Peso autom&aacute;tico (1 a 100)</label><input id="distributionWeight" type="range" min="1" max="100" value="25"><output id="weightValue">25</output><small id="weightSummary"></small></div><div class="form-field"><label>Status</label><select id="status"><option value="draft">Rascunho</option><option value="published">Ativo</option></select></div><div class="form-field"><label>Prioridade</label><input id="priority" type="number" min="1" value="1"></div><button class="btn primary" id="submitAd">Salvar anúncio</button></form></div></div>`}
function adMedia(ad){if(!ad?.mediaUrl)return '<span class="ads-thumb">AD</span>';return ad.mediaType==='Video'?`<video class="ads-thumb-media" src="${esc(ad.mediaUrl)}" muted></video>`:`<img class="ads-thumb-media" src="${esc(ad.mediaUrl)}" alt="">`}
let contextMenu=null;function closeContext(){contextMenu?.remove();contextMenu=null}function showContext(event,items,insideFrame=false){event.preventDefault();closeContext();contextMenu=document.createElement('div');contextMenu.className='admin-context-menu';contextMenu.innerHTML=items.map(item=>`<button type="button" class="${item.danger?'danger':''}" data-action="${item.id}">${item.label}</button>`).join('');const rect=insideFrame?$('#site').getBoundingClientRect():{left:0,top:0};contextMenu.style.left=`${Math.min(rect.left+event.clientX,innerWidth-220)}px`;contextMenu.style.top=`${Math.min(rect.top+event.clientY,innerHeight-220)}px`;document.body.append(contextMenu);contextMenu.onclick=click=>{const id=click.target.closest('[data-action]')?.dataset.action;if(!id)return;const action=items.find(item=>item.id===id);closeContext();action?.run()}}
function removeFromPosition(position){delete placementMap()[position];renderPositions();mountZones();queueAssignmentSave()}
function removeAdEverywhere(adId){for(const deviceMaps of Object.values(placements))for(const map of Object.values(deviceMaps))for(const key of Object.keys(map))if(map[key]===adId)delete map[key];renderPositions();mountZones();queueAssignmentSave()}
const distributionPages=[['Início',1],['Produtos e busca',26],['Categorias',51],['Produto',76]];
function updateDistributionUi(){
  const mode=$('#distributionMode')?.value||'manual',weight=Math.max(1,Math.min(100,Number($('#distributionWeight')?.value)||25)),field=$('#weightField');
  if(!field)return;field.hidden=mode!=='weighted';$('#weightValue').textContent=weight;
  const pages=distributionPages.filter(([,minimum])=>weight>=minimum).map(([name])=>name),times=Math.ceil(weight/25);
  $('#weightSummary').innerHTML=`Pode aparecer em <b>${pages.join(', ')}</b>, ocupando at&eacute; <b>${times} ${times===1?'posi&ccedil;&atilde;o':'posi&ccedil;&otilde;es'}</b> por p&aacute;gina. A chance de exibi&ccedil;&atilde;o &eacute; proporcional ao peso em rela&ccedil;&atilde;o aos outros an&uacute;ncios autom&aacute;ticos.`;
}function openAdModal(ad=null){
  $('#form').reset();
  $('#editId').value=ad?.id||'';
  $('#modal-title').textContent=ad?'Editar anúncio':'Novo anúncio';
  $('#submitAd').textContent=ad?'Salvar alterações':'Criar anúncio';
  $('#name').value=ad?.name||'';
  $('#link').value=ad?.linkUrl||'promocoes.html';$('#productSlug').value=ad?.productSlug||'';
  $('#status').value=ad?.status||'draft';
  $('#priority').value=ad?.priority||1;
  $('#distributionMode').value=ad?.distributionMode||'manual';
  $('#distributionWeight').value=ad?.distributionWeight||25;
  updateDistributionUi();
  $('#mediaType').value=ad?.mediaType||'Imagem';
  $('#publicTitle').value=ad?.publicTitle||ad?.name||'';
  $('#adLabel').value=ad?.adLabel||'PUBLICIDADE · SHOPLAB ADS';
  $('#ctaText').value=ad?.ctaText||'Saiba mais';
  $('#ctaColor').value=ad?.ctaColor||'#075fce';$('#oldPriceText').value=ad?.oldPriceText||'';$('#oldPriceColor').value=ad?.oldPriceColor||'#71807c';$('#currentPriceText').value=ad?.currentPriceText||'';$('#currentPriceColor').value=ad?.currentPriceColor||'#087c70';
  $('#showHeader').checked=ad?.showHeader!==false;
  $('#dismissible').checked=ad?.dismissible!==false;
  $('#dismissMinutes').value=ad?.dismissMinutes||30;
  $('#media').required=!ad;
  $('#modal').classList.add('open');
}
async function persistAdStatus(ad,status){
  const form=new FormData();
  form.append('name',ad.name);
  form.append('linkUrl',ad.linkUrl||'promocoes.html');
  form.append('status',status==='published'?'active':'draft');
  form.append('priority',ad.priority||1);form.append('distributionMode',ad.distributionMode||'manual');form.append('distributionWeight',ad.distributionWeight||25);form.append('publicTitle',ad.publicTitle||ad.name);form.append('targetKeywords',ad.targetKeywords||'');form.append('adLabel',ad.adLabel||'PUBLICIDADE · SHOPLAB ADS');form.append('showHeader',ad.showHeader===false?'0':'1');form.append('dismissible',ad.dismissible===false?'0':'1');form.append('dismissMinutes',ad.dismissMinutes||30);form.append('ctaText',ad.ctaText||'Saiba mais');form.append('ctaColor',ad.ctaColor||'#075fce');form.append('oldPriceText',ad.oldPriceText||'');form.append('oldPriceColor',ad.oldPriceColor||'#71807c');form.append('currentPriceText',ad.currentPriceText||'');form.append('currentPriceColor',ad.currentPriceColor||'#087c70');
  await api(`/api/v1/admin/shoplab-ads/${encodeURIComponent(ad.id)}`,{method:'PUT',body:form});
  ad.status=status;
  renderLibrary();
  show(status==='published'?'Anúncio ativado e salvo.':'Anúncio movido para rascunho.');
}
function renderLibrary(){
  const host=$('#ads');
  host.innerHTML=ads.map(ad=>`<article class="ads-ad ${selectedAd===ad.id?'selected':''}" draggable="true" data-ad="${ad.id}">${adMedia(ad)}<div><b>${esc(ad.name)}</b><small>${esc(ad.mediaType||'Imagem')} &middot; ${ad.status==='published'?'Ativo':'Rascunho'}</small></div><button type="button" class="ads-edit" data-edit>Editar</button><i aria-hidden="true">⋮⋮</i></article>`).join('')||'<p class="muted">Crie o primeiro anúncio.</p>';
  $$('[data-ad]',host).forEach(card=>{
    const get=()=>ads.find(ad=>ad.id===card.dataset.ad);
    card.querySelector('[data-edit]').onclick=e=>{e.stopPropagation();openAdModal(get())};
    card.onclick=e=>{if(e.target.closest('[data-edit]'))return;selectedAd=card.dataset.ad;renderLibrary()};
    card.ondragstart=e=>{draggingAd=card.dataset.ad;selectedAd=draggingAd;card.classList.add('is-dragging');e.dataTransfer.setData('text/plain',draggingAd);e.dataTransfer.setData('text/shoplab-ad',draggingAd);e.dataTransfer.effectAllowed='copy'};
    card.ondragend=()=>{card.classList.remove('is-dragging');draggingAd='';renderLibrary();mountZones()};
    card.oncontextmenu=e=>{const ad=get();showContext(e,[{id:'edit',label:'Editar anúncio',run:()=>openAdModal(ad)},{id:'select',label:'Selecionar anúncio',run:()=>{selectedAd=ad.id;renderLibrary()}},{id:'toggle',label:ad.status==='published'?'Mover para rascunho':'Ativar anúncio',run:()=>persistAdStatus(ad,ad.status==='published'?'draft':'published')},{id:'unplace',label:'Remover de todas as posições',danger:true,run:()=>removeAdEverywhere(ad.id)}])};
  });
}function renderPositions(){const host=$('#position-list');host.innerHTML=currentSlots().map(([id,name,size])=>{const ad=ads.find(item=>item.id===placementMap()[id]);return `<button class="ads-position ${ad?'used':''}" data-position="${id}"><b>${esc(name)}</b><small>${size}${ad?` &middot; ${esc(ad.name)}`:''}</small></button>`}).join('')}
function zoneTarget(doc,id){const main=doc.querySelector('#conteudo')||doc.querySelector('main'),footer=doc.querySelector('.footer');if(id==='top'||id==='menu')return doc.querySelector('.header')||main;if(id==='footer'||id==='product_footer')return footer||main;if(id==='home_categories')return doc.querySelector('.home-category-section')||doc.querySelector('.mobile-home-categories')||main;if(id==='home_sections'||id==='sections')return doc.querySelectorAll('.home-section')[1]||doc.querySelector('.home-section')||main;if(id==='products')return doc.querySelector('.products')||doc.querySelector('.home-section')||main;if(id==='grid_sidebar')return doc.querySelector('#listing-products')||doc.querySelector('.catalog-results .products')||doc.querySelector('.products')||main;if(id==='product_top')return doc.querySelector('.product-page-hero')||main;if(id==='product_middle')return doc.querySelector('#product-related')||main;return main}
function insertZone(doc,slot){const [id,name,size]=slot,target=zoneTarget(doc,id);if(!target)return;const zone=doc.createElement('div');zone.className='shoplab-editor-zone';zone.dataset.position=id;const assigned=()=>ads.find(item=>item.id===placementMap()[id]);const paint=(ad,preview=false)=>{if(ad?.mediaUrl){const media=ad.mediaType==='Video'?`<video src="${esc(ad.mediaUrl)}" muted autoplay loop playsinline></video>`:`<img src="${esc(ad.mediaUrl)}" alt="${esc(ad.name)}">`;zone.innerHTML=`<button class="shoplab-zone-remove" type="button" title="Remover desta posicao">&times;</button><div class="shoplab-zone-creative ${preview?'is-preview':''}">${media}<span>${esc(ad.name)}</span></div>`;zone.querySelector('.shoplab-zone-remove').onclick=e=>{e.stopPropagation();removeFromPosition(id)}}else zone.innerHTML=`<strong>${esc(name)}</strong><small>${size}</small>`};paint(assigned());if(id==='top'||id==='menu'||id==='home_categories'||id==='home_sections'||id==='sections'||id==='products'||id==='product_top')target.insertAdjacentElement('afterend',zone);else target.insertAdjacentElement('beforebegin',zone);zone.ondragenter=e=>{e.preventDefault();const ad=ads.find(item=>item.id===(draggingAd||selectedAd));if(ad){paint(ad,true);zone.classList.add('is-over')}};zone.ondragover=e=>{e.preventDefault();e.dataTransfer.dropEffect='copy'};zone.ondragleave=e=>{if(!zone.contains(e.relatedTarget)){zone.classList.remove('is-over');paint(assigned())}};zone.ondrop=e=>{e.preventDefault();const adId=e.dataTransfer.getData('text/shoplab-ad')||e.dataTransfer.getData('text/plain')||draggingAd||selectedAd;if(adId){placementMap()[id]=adId;draggingAd='';renderPositions();mountZones();queueAssignmentSave()}};zone.onclick=e=>{if(e.target.closest('.shoplab-zone-remove'))return;if(selectedAd){placementMap()[id]=selectedAd;renderPositions();mountZones();queueAssignmentSave()}};zone.oncontextmenu=e=>{const ad=assigned(),items=[];if(ad)items.push({id:'remove',label:'Remover an&uacute;ncio desta posi&ccedil;&atilde;o',danger:true,run:()=>removeFromPosition(id)});else items.push({id:'empty',label:'Posi&ccedil;&atilde;o vazia',run:()=>{}});showContext(e,items,true)}}function mountZones(attempt=0){const frame=$('#site'),doc=frame?.contentDocument;if(!doc)return;doc.querySelectorAll('.shoplab-editor-zone,#shoplab-editor-style').forEach(node=>node.remove());const style=doc.createElement('style');style.id='shoplab-editor-style';style.textContent=`.shoplab-editor-zone{box-sizing:border-box;min-height:${device==='mobile'?'48':'72'}px;margin:12px auto;width:${device==='mobile'?'calc(100% - 20px)':'min(92%,970px)'};border:2px dashed #0a9b82;background:#e5f8f3e8;color:#087565;display:flex;flex-direction:column;align-items:center;justify-content:center;font:700 13px/1.2 Arial;cursor:copy;position:relative;z-index:1}.shoplab-editor-zone small{font-size:11px;font-weight:500;margin-top:4px}.shoplab-editor-zone.is-over{background:#bcecdf;border-style:solid}.shoplab-zone-creative{position:absolute;inset:0;overflow:hidden;border-radius:4px;background:#092e29}.shoplab-zone-creative img,.shoplab-zone-creative video{width:100%;height:100%;object-fit:contain;display:block;background:#fff}.shoplab-zone-creative span{position:absolute;left:8px;bottom:6px;padding:3px 6px;border-radius:4px;background:#052e29cc;color:#fff;font-size:10px}.shoplab-zone-creative.is-preview{outline:3px solid #14b89b;outline-offset:-3px}.shoplab-zone-remove{position:absolute;z-index:3;right:5px;top:5px;width:24px;height:24px;border:0;border-radius:50%;background:#fff;color:#a42525;font-size:18px;line-height:20px;cursor:pointer;box-shadow:0 2px 8px #0003}`;doc.head.append(style);currentSlots().forEach(slot=>insertZone(doc,slot));if(attempt<5&&doc.querySelectorAll('.shoplab-editor-zone').length<currentSlots().length)setTimeout(()=>mountZones(attempt+1),500)}
function refreshPreview(){const frame=$('#site'),option=$('#page').selectedOptions[0];pageKind=option.dataset.kind;renderPositions();frame.src='about:blank';requestAnimationFrame(()=>{frame.src=`../${option.value}`})}
function scheduleZoneMounts(){[400,1200,2500,5000].forEach(delay=>setTimeout(()=>mountZones(),delay))}let assignmentSaveTimer;function assignmentItems(){const items=[];for(const targetDevice of ['desktop','mobile'])for(const targetPage of ['home','products','category','product'])for(const [positionKey,adId] of Object.entries(placementMap(targetDevice,targetPage)))items.push({adId,device:targetDevice,pageKind:targetPage,positionKey,categorySlug:''});return items}async function saveAssignments(){return api('/api/v1/admin/shoplab-ad-assignments',{method:'PUT',body:JSON.stringify({items:assignmentItems()})})}function queueAssignmentSave(){clearTimeout(assignmentSaveTimer);assignmentSaveTimer=setTimeout(async()=>{try{await saveAssignments();ads=await loadAds();renderLibrary();show('Posição salva e publicada automaticamente.')}catch(error){show(error.message)}},350)}function bind(){
  const frame=$('#site');
  frame.addEventListener('load',scheduleZoneMounts);
  $$('[data-device]').forEach(button=>button.onclick=()=>{device=button.dataset.device;$$('[data-device]').forEach(item=>item.classList.toggle('active',item===button));$('#frame').className=`ads-frame is-${device}`;renderPositions();refreshPreview()});
  $('#page').onchange=refreshPreview;
  $('#refresh').onclick=refreshPreview;
  $('#new').onclick=()=>openAdModal();
  $('#close').onclick=()=>$('#modal').classList.remove('open');
  $('#distributionMode').onchange=updateDistributionUi;
  $('#distributionWeight').oninput=updateDistributionUi;
  $('#form').onsubmit=async e=>{
    e.preventDefault();
    try{
      const form=new FormData(),file=$('#media').files[0],editId=$('#editId').value;
      form.append('name',$('#name').value.trim());
      form.append('linkUrl',$('#link').value.trim());form.append('productSlug',$('#productSlug').value);
      form.append('status',$('#status').value==='published'?'active':'draft');
      form.append('priority',$('#priority').value);
      form.append('distributionMode',$('#distributionMode').value);
      form.append('distributionWeight',$('#distributionWeight').value);
      form.append('publicTitle',$('#publicTitle').value.trim()||$('#name').value.trim());
      form.append('adLabel',$('#adLabel').value.trim()||'PUBLICIDADE · SHOPLAB ADS');
      form.append('showHeader',$('#showHeader').checked?'1':'0');
      form.append('dismissible',$('#dismissible').checked?'1':'0');
      form.append('dismissMinutes',$('#dismissMinutes').value||'30');
      form.append('ctaText',$('#ctaText').value.trim()||'Saiba mais');
      form.append('ctaColor',$('#ctaColor').value||'#075fce');form.append('oldPriceText',$('#oldPriceText').value.trim());form.append('oldPriceColor',$('#oldPriceColor').value||'#71807c');form.append('currentPriceText',$('#currentPriceText').value.trim());form.append('currentPriceColor',$('#currentPriceColor').value||'#087c70');
      if(file)form.append('file',file);
      await api(editId?`/api/v1/admin/shoplab-ads/${encodeURIComponent(editId)}`:'/api/v1/admin/shoplab-ads',{method:editId?'PUT':'POST',body:form});
      ads=await loadAds();
      selectedAd=editId||ads[0]?.id||'';
      $('#modal').classList.remove('open');
      renderLibrary();renderPositions();mountZones();
      show(editId?'Anúncio atualizado no banco de dados.':'Anúncio salvo no banco de dados.');
    }catch(error){show(error.message)}
  };
  $('#save').onclick=async()=>{try{await saveAssignments();ads=await loadAds();renderLibrary();show('Posições salvas no banco e publicadas no site.')}catch(error){show(error.message)}};
}function show(message){const node=$('#message');node.innerHTML=message;node.className='admin-message show success'}
const numberFormat=new Intl.NumberFormat('pt-BR'),percentFormat=new Intl.NumberFormat('pt-BR',{minimumFractionDigits:2,maximumFractionDigits:2});
function setupAnalyticsUi(){const top=$('.admin-top');top.insertAdjacentHTML('afterend',`<nav class="ads-view-tabs"><button type="button" class="active" data-ads-view="positions">Posi��es</button><button type="button" data-ads-view="analytics">Analytics</button></nav>`);$('.ads-layout').insertAdjacentHTML('afterend',`<section class="ads-analytics" id="ads-analytics" hidden><header class="ads-analytics-head"><div><span class="eyebrow">DESEMPENHO</span><h2>Analytics dos an�ncios</h2><p class="muted">Impress�es vis�veis, cliques e taxa de cliques no per�odo.</p></div><label>Per�odo<select id="analytics-period"><option value="7">�ltimos 7 dias</option><option value="30" selected>�ltimos 30 dias</option><option value="90">�ltimos 90 dias</option></select></label></header><div id="analytics-content"><div class="analytics-loading">Carregando m�tricas�</div></div></section>`);$$('[data-ads-view]').forEach(button=>button.onclick=async()=>{const analytics=button.dataset.adsView==='analytics';$$('[data-ads-view]').forEach(item=>item.classList.toggle('active',item===button));$('.ads-layout').hidden=analytics;$('.ads-device-toggle').hidden=analytics;$('#ads-analytics').hidden=!analytics;$('#refresh').hidden=analytics;$('#save').hidden=analytics;top.querySelector('.eyebrow').innerHTML=analytics?'ADMINISTRA��O &nbsp;�&nbsp; AN�NCIOS &nbsp;�&nbsp; ANALYTICS':'ADMINISTRA��O &nbsp;�&nbsp; AN�NCIOS &nbsp;�&nbsp; POSI��ES';top.querySelector('.muted').textContent=analytics?'Acompanhe o desempenho e descubra quais an�ncios geram mais interesse.':'Arraste os an�ncios para os espa�os reais do site.';if(analytics)await loadAnalytics()});$('#analytics-period').onchange=loadAnalytics}
function analyticsChart(rows,days){const byDay=new Map(rows.map(row=>[row.day,row])),points=[];for(let offset=days-1;offset>=0;offset--){const date=new Date();date.setDate(date.getDate()-offset);const key=date.toISOString().slice(0,10);points.push({day:key,...(byDay.get(key)||{impressions:0,clicks:0})})}const max=Math.max(1,...points.flatMap(point=>[point.impressions,point.clicks])),coords=key=>points.map((point,index)=>`${index/(points.length-1)*100},${92-point[key]/max*78}`).join(' ');return `<div class="analytics-chart"><div class="chart-legend"><span class="impressions">Impress�es</span><span class="clicks">Cliques</span></div><svg viewBox="0 0 100 100" preserveAspectRatio="none"><line x1="0" y1="92" x2="100" y2="92"/><line x1="0" y1="53" x2="100" y2="53"/><line x1="0" y1="14" x2="100" y2="14"/><polyline class="impressions" points="${coords('impressions')}"/><polyline class="clicks" points="${coords('clicks')}"/></svg><div class="chart-dates"><span>${new Date(points[0].day+'T12:00:00').toLocaleDateString('pt-BR')}</span><span>${new Date(points.at(-1).day+'T12:00:00').toLocaleDateString('pt-BR')}</span></div></div>`}
function analyticsBreakdown(rows){const labels={desktop:'Desktop',mobile:'Mobile'},total=Math.max(1,...rows.map(row=>row.impressions));return `<div class="breakdown-list">${rows.map(row=>`<div><span><strong>${labels[row.device]||row.device}</strong><small>${numberFormat.format(row.impressions)} impress�es � ${percentFormat.format(row.ctr)}% CTR</small></span><i><b style="width:${row.impressions/total*100}%"></b></i></div>`).join('')||'<p class="muted">Os dados aparecer�o quando os an�ncios forem exibidos.</p>'}</div>`}
async function loadAnalytics(){const host=$('#analytics-content');host.innerHTML='<div class="analytics-loading">Carregando m�tricas�</div>';try{const data=await api(`/api/v1/admin/shoplab-ads/analytics?days=${$('#analytics-period').value}`),t=data.totals;host.innerHTML=`<div class="analytics-kpis"><article><span>Impress�es</span><strong>${numberFormat.format(t.impressions)}</strong><small>Exibi��es com 50% vis�vel</small></article><article><span>Cliques</span><strong>${numberFormat.format(t.clicks)}</strong><small>Acessos ao destino</small></article><article><span>CTR</span><strong>${percentFormat.format(t.ctr)}%</strong><small>Cliques � impress�es</small></article><article><span>Alcance</span><strong>${numberFormat.format(t.reach)}</strong><small>Sess�es �nicas estimadas</small></article></div><div class="analytics-panel"><span class="eyebrow">EVOLU��O</span><h3>Desempenho di�rio</h3>${analyticsChart(data.daily,data.days)}</div><div class="analytics-grid"><div class="analytics-panel"><span class="eyebrow">CRIATIVOS</span><h3>Desempenho por an�ncio</h3><div class="analytics-table-wrap"><table class="analytics-table"><thead><tr><th>An�ncio</th><th>Status</th><th>Impress�es</th><th>Cliques</th><th>CTR</th></tr></thead><tbody>${data.ads.map(row=>`<tr><td><strong>${esc(row.name)}</strong></td><td><span class="analytics-status ${row.status}">${row.status==='active'?'Ativo':row.status==='paused'?'Pausado':'Rascunho'}</span></td><td>${numberFormat.format(row.impressions)}</td><td>${numberFormat.format(row.clicks)}</td><td><strong>${percentFormat.format(row.ctr)}%</strong></td></tr>`).join('')||'<tr><td colspan="5">Nenhum an�ncio cadastrado.</td></tr>'}</tbody></table></div></div><div class="analytics-panel"><span class="eyebrow">P�BLICO</span><h3>Por dispositivo</h3>${analyticsBreakdown(data.devices)}</div></div>`}catch(error){host.innerHTML=`<div class="analytics-empty"><strong>N�o foi poss�vel carregar o analytics.</strong><p>${esc(error.message)}</p><button class="btn ghost" id="retry-analytics">Tentar novamente</button></div>`;$('#retry-analytics').onclick=loadAnalytics}}
async function loadAds(){const rows=await api('/api/v1/admin/shoplab-ads');return rows.map(row=>({id:row.id,name:row.name,status:row.status==='active'?'published':'draft',mediaType:row.mediaType==='video'?'Video':'Imagem',mediaUrl:row.storageKey?`${C.API_BASE_URL}/media/${encodeURIComponent(row.storageKey)}`:'',linkUrl:row.linkUrl||'',priority:Number(row.priority)||1,publicTitle:row.publicTitle||row.name,adLabel:row.adLabel||'PUBLICIDADE · SHOPLAB ADS',showHeader:Number(row.showHeader)!==0,dismissible:Number(row.dismissible)!==0,dismissMinutes:Number(row.dismissMinutes)||30,ctaText:row.ctaText||'Saiba mais',ctaColor:row.ctaColor||'#075fce',productSlug:row.productSlug||'',oldPriceText:row.oldPriceText||'',oldPriceColor:row.oldPriceColor||'#71807c',currentPriceText:row.currentPriceText||'',currentPriceColor:row.currentPriceColor||'#087c70',targetKeywords:row.targetKeywords||'',distributionMode:row.distributionMode==='weighted'?'weighted':'manual',distributionWeight:Math.max(1,Math.min(100,Number(row.distributionWeight)||25))}))}
async function loadAdProductOptions(){const products=await api('/api/v1/products?limit=50').catch(()=>[]);const list=$('#shoplab-product-options');list.innerHTML=products.map(product=>'<option value="'+esc(product.slug)+'" label="'+esc(product.name)+'"></option>').join('')}async function init(){await api('/api/v1/admin/auth/session');shell();setupAnalyticsUi();await loadAdProductOptions();ads=await loadAds();const assignments=await api('/api/v1/admin/shoplab-ad-assignments');placements={desktop:{},mobile:{}};for(const item of assignments)placementMap(item.device,item.pageKind)[item.positionKey]=item.adId;renderLibrary();renderPositions();bind()}init().catch(error=>{document.body.innerHTML=`<main class="admin-main"><h1>Shoplab Ads</h1><p>${esc(error.message)}</p></main>`});
